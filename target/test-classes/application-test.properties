# Test configuration
spring.application.name=Agify Age Estimator Test
server.port=0

# Agify API Configuration for testing
agify.api.url=https://api.agify.io

# Disable logging for tests
logging.level.com.example.agifyapp=WARN
logging.level.org.springframework.web=WARN
logging.level.org.springframework.boot=WARN

# Disable Thymeleaf cache for tests
spring.thymeleaf.cache=false
